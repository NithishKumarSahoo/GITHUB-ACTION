name: Hello world workflow
on:
  push:
    branches: [ main ]
  pull_request:

jobs:
    hello_worlds:
      runs-on: ubuntu-latest
      steps:
        - name: Print Hello world
          run: echo "Hello Universe"